[project]
name = "grades"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jastk45", email = "jastk45@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
grades = "grades:main"

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"
